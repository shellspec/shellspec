FROM debian:buster
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN uname -a && cat /etc/debian_version && dpkg -l dash

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "dash" ]
