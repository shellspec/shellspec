# sourced script can not stop with return
# xor not supported
FROM lpenz/debian-woody-i386
ENV SH="ash" DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes
RUN apt-get update && apt-get -y --force-yes install ash
