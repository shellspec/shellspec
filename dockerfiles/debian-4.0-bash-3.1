FROM lpenz/debian-etch-i386
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN uname -a && cat /etc/debian_version && dpkg -l bash

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "bash" ]
