FROM debian:8.11-slim
RUN export DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes \
 && apt-get update && apt-get -y install zsh sudo \
 && echo "ALL ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers \
 && useradd -m user
USER user
ENV SH="zsh"
