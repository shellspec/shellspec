FROM openwrt:12.09
RUN mkdir -p /var/lock /home \
 && opkg update \
 && opkg install sudo shadow-useradd \
 && echo "ALL ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers \
 && useradd -m user
USER user
ENV SH="busybox ash"
