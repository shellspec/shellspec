FROM lpenz/debian-sarge-i386
RUN apt-get update && apt-get -y install posh sudo \
 && echo "ALL ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers \
 && adduser --disabled-password --gecos "" user
USER user
ENV SH="posh"
