FROM debian:9
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN apt-get -y install yash
RUN uname -a && cat /etc/debian_version && dpkg -l yash

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "yash" ]
