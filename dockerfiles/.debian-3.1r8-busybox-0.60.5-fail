FROM lpenz/debian-sarge-i386
RUN apt-get update && apt-get -y install sudo busybox-static \
 && ln -s /bin/busybox /bin/ash \
 && echo "ALL ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers \
 && groupadd user && useradd -m user -g user
USER user
ENV SH="ash"
