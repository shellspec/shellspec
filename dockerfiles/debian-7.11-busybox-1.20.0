FROM lpenz/debian-wheezy-amd64-minbase
RUN useradd -m user \
 && export DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes \
 && apt-get update && apt-get -y install busybox \
 && /bin/busybox --install -s
USER user
ENV SH="ash"
