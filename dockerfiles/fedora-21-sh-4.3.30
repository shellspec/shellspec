FROM fedora:21
RUN rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-21-x86_64 \
 && yum install -y sudo tar \
 && echo "ALL ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers \
 && useradd -m user
USER user
ENV SH="sh"
