FROM debian:7
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN apt-get -y install posh
RUN uname -a && cat /etc/debian_version && dpkg -l posh

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "posh" ]
