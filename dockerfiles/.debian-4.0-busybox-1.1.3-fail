# Built-in commands can not redefine.
FROM lpenz/debian-etch-i386
ENV SH="busybox ash" DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes
RUN apt-get update && apt-get -y --force-yes install busybox
