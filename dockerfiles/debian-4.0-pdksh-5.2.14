FROM lpenz/debian-etch-i386
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN apt-get -y --force-yes install pdksh
RUN uname -a && cat /etc/debian_version && dpkg -l pdksh

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "pdksh" ]
