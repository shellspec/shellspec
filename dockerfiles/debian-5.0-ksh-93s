FROM lpenz/debian-lenny-i386
ENV DEBIAN_FRONTEND noninteractive
ENV DEBCONF_NOWARNINGS yes
RUN apt-get update

RUN apt-get -y --force-yes install ksh
RUN uname -a && cat /etc/debian_version && dpkg -l ksh

WORKDIR /shellspec
COPY ./ ./
CMD [ "./shellspec", "--shell", "ksh" ]
