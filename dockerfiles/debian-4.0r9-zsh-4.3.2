FROM lpenz/debian-etch-i386-minbase
RUN useradd -m user \
 && export DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes \
 && apt-get update && apt-get -y install zsh && touch /home/user/.zshrc
USER user
ENV SH="zsh"
